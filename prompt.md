a TUI written in golang that uses bubbletea and its framework (and its other products like wish, lip gloss, crush, harmonica, etc..). lets use those to develop a versatile, flexible, professional, feature packed tui that will be used to monitor and execute the data ingestion (in bulk and in particular) from api endpoints such as congress.gov, govinfo.gov, openstates.org, openlegislation (if available), data.org, census data, FRED economic data, APC housing survey data, FBI crime data, free stock price and economic data, free crime data, free FBI and CIA declassified documents, free CBO budget docs, free SEC filings, free investment trades made by politicians (free filings for politicians when they make investment trades). lets have this TUI  help us manage our api keys (theres a go library by bubbletea for that), and help us manage, run, schedule, debug our ingestion scripts and sql migration files. lets have the TUI save sql migration files for each of the endpoints or each of the tables in the schema in its memory and also api keys and it will help manage api endpoint schemas so we know what to ingest (like postman). and it will help us reverse engineer api endpoints to figure out how to write scripts to ingest the data. have this TUI have access to the openrouter sdk and write scripts to have the free ai models that they offer to help us accomplish some of our tasks and to offer support in a chat interface that ships with the TUI. have the ai agent have RAG inference and have knwoledge of all of the files the user has saved and can help interface with the api endpoints and has the puppetteer mcp server and many other mcp servers so it can be useful. make the config of the ai configurable and extendable and also make sure they have access to tools or can use gemini, qwen, claude code, codex, opencode, cline, roo-code, kilo code, continue.dev, etc... please make sure there are code decorators in the code and comments throughout. make sure there is a header in each file and robust error handling. 
